# COMP2911 Acceptance Test Makefile
# Benjamin James Wright (bwright@cse.unsw.edu.au)
# 2012.04.06 (ISO 8601)
# This makefile allows you to build the entire project which includes the
# framework and test or compile just a single component such as the unstable
# tests. Here are some examwples:
#     make all       # Builds everything in the project.
#     make framework # Builds just the project framework.
#     make tests     # Builds only the tests in the project.
#     make unstable  # Builds only the unstable tests in the project.
#     make clean     # Cleans the entire project.

all:
framework:
tests:

unstable:
testing:
stable:

clean:
